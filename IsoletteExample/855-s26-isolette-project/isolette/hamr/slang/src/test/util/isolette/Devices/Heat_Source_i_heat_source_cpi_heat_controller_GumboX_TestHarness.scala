// #Sireum

package isolette.Devices

import org.sireum._
import isolette._
import isolette.GumboXUtil.GumboXResult

// Do not edit this file as it will be overwritten if HAMR codegen is rerun
@msig trait Heat_Source_i_heat_source_cpi_heat_controller_GumboX_TestHarness extends Heat_Source_i_heat_source_cpi_heat_controller_TestApi {
  def verbose: B

  def testComputeCBJ(json: String): GumboXResult.Type = {
    isolette.JSON.toDevicesHeat_Source_i_heat_source_cpi_heat_controller_PreState_Container(json) match {
      case Either.Left(o) => return testComputeCBV(o)
      case Either.Right(msg) => halt(msg.string)
    }
  }

  def testComputeCBV(o: Heat_Source_i_heat_source_cpi_heat_controller_PreState_Container): GumboXResult.Type = {
    return testComputeCB(o.api_heat_control)
  }

  /** Contract-based test harness for the compute entry point
    * @param api_heat_control incoming data port
    */
  def testComputeCB(
      api_heat_control: Isolette_Data_Model.On_Off.Type): GumboXResult.Type = {

    // [SaveInLocal]: retrieve and save the current (input) values of GUMBO-declared local state variables as retrieved from the component state
    //   heat_controller does not have incoming ports or state variables

    // [CheckPre]: check/filter based on pre-condition.
    //   heat_controller's compute entry point does not have top level assume clauses

    // [PutInPorts]: put values on the input ports
    put_heat_control(api_heat_control)

    if (verbose) {
      println(st"""Pre State Values:
                  |  api_heat_control = ${api_heat_control.string}""".render)
    }

    // [InvokeEntryPoint]: invoke the entry point test method
    testCompute()

    // [RetrieveOutState]: retrieve values of the output ports via get operations and GUMBO declared local state variable
    //   heat_controller does not have outgoing ports or state variables

    // [CheckPost]: invoke the oracle function
    //   heat_controller does not contain guarantee clauses for its compute entrypoint
    val result: GumboXResult.Type = GumboXResult.Post_Condition_Pass

    return result
  }
}
